{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Only the bundled connection window can inspect and start the local runtime.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
